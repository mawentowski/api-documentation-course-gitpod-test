image: gitpod/workspace-mongodb

tasks:
  - name: run back-end
    command: |
      cd server
      npm install
      npm run start
ports:
  - name: back-end
    port: 8080
    onOpen: ignore
    visibility: public
